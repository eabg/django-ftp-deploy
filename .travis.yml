language: python
python:
  - "2.7"
env:
    - DJANGO_VERSION=1.5.11
install:
  - pip install Django==$DJANGO_VERSION
  - pip install -r requirements/travis.txt
  - export PYTHONPATH=${TRAVIS_BUILD_DIR}+'/django-ftp-deploy/'
script:
  - cd tests
  - fab testci
